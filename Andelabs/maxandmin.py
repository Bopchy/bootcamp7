def max_min(A):
	type(A) == list # defining input as list
	for item in range (A[0], (len(A) - 1)
	# defining range of input 
		a = max # a stores maximum number
		b = min # b stores minimum number 
	return [a, b]

print max_min([1, 2, 3, 4])